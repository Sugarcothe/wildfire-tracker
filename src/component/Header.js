import React from 'react'

const Header = () => {
  return (
    <Header className='header'>
      <h1><Icon icon={locationIcon}/>Wildfire Tracker (Data fetched from NASA)</h1>
    </Head>
  )
}

export default Header
